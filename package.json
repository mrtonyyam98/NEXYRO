{
  "name": "nexyro",
  "version": "1.0.0",
  "description": "Full stack project management app (PERN Stack)",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

